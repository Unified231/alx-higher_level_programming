# This code adds two numbers and prints the result.

# Import the add function from the add_0 module.
from add_0 import add

# Define two variables to store the numbers to be added.
a = 1
b = 2

# Call the add function to add the two numbers.
sum = add(a, b)

# Print the result of the addition.
print("{} + {} = {}".format(a, b, sum))

